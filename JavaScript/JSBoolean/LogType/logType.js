
let dataType = typeof data;

switch (typeof data) {
    case "undefined":
    case "null":
    case "number":
        if (dataType ==='number'){
         if (isNaN(data)) {

        }
    }
    console.log(dataType + "!")
    break;
}
